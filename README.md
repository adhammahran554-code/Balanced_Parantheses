# Balanced_Parantheses
This project implements a **Stack using a linked list in C++** and uses it to check whether the parentheses in an expression are balanced. The program pushes opening brackets into the stack and removes them when matching closing brackets appear. If all brackets match correctly and the stack becomes empty, the expression is balanced.
